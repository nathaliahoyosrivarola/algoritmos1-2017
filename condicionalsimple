#include "stdafx.h"
#include <iostream>
#include "conio.h"

using namespace std;

void main()
{
	int edad;
	cout<<"introduzca la edad del ciudadano:  ";
	cin>>edad;
	if(edad>=18)
		cout<<"El ciudadano puede votar."<<endl;
	if(edad<18)
		cout<<"El ciudadano no puede votar."<<endl;
	getch();
}
